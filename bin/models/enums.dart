enum Position { goalkeeper, defender, midfielder, forward }

enum WeatherCondition { sunny, rainy, snowy, windy, clear }

enum Strategy {
  offensive,
  defensive,
  balanced,
  counterAttack,
  pressing,
  parkTheBus,
}

enum RefereeStyle { lenient, balanced, strict }
